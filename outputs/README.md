## Outputs

This folder contains sample visualizations generated by the pipeline.

Note:
- Trained model files (.pkl) are not tracked to keep the repository lightweight.
- Run `code.py` to regenerate all outputs.